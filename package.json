{
  "name": "claude-usage-streamdeck",
  "version": "1.2.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Stream Deck plugin showing Claude session/weekly usage",
  "scripts": {
    "build": "esbuild src/plugin.ts --bundle --platform=node --target=node20 --format=cjs --outfile=com.local.claude-usage.sdPlugin/bin/plugin.js"
  },
  "dependencies": {
    "@elgato/streamdeck": "^2.1.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@elgato/cli": "^1.7.4",
    "esbuild": "^0.28.0"
  }
}
